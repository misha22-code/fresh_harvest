// lib/config/app_constants.dart
import 'package:flutter/material.dart';

// ─── Colors ───────────────────────────────────────────────────────────────────

const Color kPrimaryColor = Color(0xFF2E7D32);
const Color kPrimaryLightColor = Color(0xFF4CAF50);
const Color kAccentColor = Color(0xFF8BC34A);
const Color kBackgroundColor = Color(0xFFF5F5F5);
const Color kWhiteColor = Color(0xFFFFFFFF);
const Color kTextPrimary = Color(0xFF212121);
const Color kTextSecondary = Color(0xFF757575);
const Color kBorderColor = Color(0xFFE0E0E0);
const Color kShadowColor = Color(0x1A000000);
const Color kSuccessColor = Color(0xFF388E3C);
const Color kWarningColor = Color(0xFFFF9800);
const Color kErrorColor = Color(0xFFD32F2F);

// ─── Spacing ──────────────────────────────────────────────────────────────────

const double kPadding = 16.0;
const double kPaddingSmall = 8.0;
const double kPaddingLarge = 24.0;

// ─── Radius ──────────────────────────────────────────────────────────────────

const double kCardRadius = 12.0;
const double kButtonRadius = 8.0;

// ─── Mock Delay ─────────────────────────────────────────────────────────────

const Duration kMockDelay = Duration(milliseconds: 500);